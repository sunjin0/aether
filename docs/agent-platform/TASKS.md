# Agent 平台任务记录

> 状态：已实现能力记录
> 更新日期：2026-07-15
> 范围：仅记录已有实现和可执行验证项；不保留未经确认的版本路线、预留功能或未来任务规划。

---

## 1. 数据模型与管理能力

- [x] 创建模型供应商、Agent 定义、MCP 服务、工具、工具绑定、会话、消息、运行记录和工具调用日志的数据模型。
- [x] 创建对应 DTO、VO、Mapper 和 Service 接口。
- [x] 提供模型供应商、Agent、MCP 服务、工具、工具绑定、会话、运行记录和工具调用日志的管理接口。
- [x] 将 SQL 收敛为 MySQL/PostgreSQL 各两个文件：`001-schema.sql` 和 `002-data.sql`。
- [x] 采用逻辑删除，`BaseEntity.deleted` 已配置 MyBatis-Plus `@TableLogic`。

## 2. 聊天与流式响应

- [x] 实现 OpenAI 兼容模型客户端和普通聊天接口。
- [x] 实现会话自动创建、上下文组装、消息和运行记录持久化。
- [x] 实现 SSE 流式聊天，支持 `message`、`tool_call`、`error`、`done` 事件。
- [x] 在 `done` 事件中返回最终消息、推理内容、模型和 Token 统计字段。
- [x] 保存 assistant 推理内容和推理 Token 数据。

## 3. MCP 与工具调用

- [x] 接入 HTTP、SSE、Streamable HTTP MCP 服务配置与调用。
- [x] 支持 MCP 服务可用性检测、认证 token 更新和调用异常处理。
- [x] 将持久化工具模型切换为 MCP 工具：关联 MCP 服务、工具名和 `inputSchema`。
- [x] 支持 Agent 与工具绑定、调用优先级和启用状态控制。
- [x] 支持将绑定工具以 `tools` 和 `tool_choice=auto` 暴露给模型。
- [x] 工具调用结果回填模型上下文，保留 assistant `tool_calls` 和 tool `tool_call_id`。
- [x] 在工具调用前创建运行记录，并持久化工具调用审计日志。
- [x] 支持工具业务分类、工具调用统计和工具中心筛选聚合。
- [x] 新增内建 `ask_user` 工具和统一内建工具注册机制。
- [x] 新增工具调用风险审查与确认机制。

## 4. 交互式提问与会话审计

- [x] 支持交互式提问和用户回复消息。
- [x] 保存消息类型、交互状态、问题配置、父消息、回复时间和过期时间。
- [x] 支持会话生命周期查询和消息统计。
- [x] 会话详情、消息、关闭和删除接口校验当前用户归属。

## 5. 权限与审计

- [x] 为 Agent、模型供应商、工具等管理接口配置 `@Permission` 权限。
- [x] 记录每次 Agent 调用的运行记录、模型用量、耗时和错误信息。
- [x] 记录工具调用参数、执行结果、耗时和安全拦截状态。

## 6. 预留功能

### 6.1 工作流

- [x] 保留 `agent_workflow` 表、实体、VO、Mapper、Service 和 Service 实现。
- [ ] 实现工作流管理与执行能力。

### 6.2 知识库

- [x] 保留 `agent_knowledge_base`、`agent_document` 表及对应实体、VO、Mapper、Service 和 Service 实现。
- [x] 新增 PostgreSQL `pgvector` 基础结构：`agent_document_chunk`、1536 维 HNSW 索引及 MyBatis-Plus 基础分层。
- [ ] 实现知识库管理、文档处理与检索能力。

## 7. 可执行验证清单

- [x] 全量编译通过（JDK 17）。
- [x] 已有单元测试通过。
- [ ] 在目标环境执行对应数据库的 `001-schema.sql` 和 `002-data.sql` 并验证建表/种子数据结果。
- [ ] 使用有效模型供应商和 MCP 服务进行聊天、SSE、工具调用、交互式提问的端到端验证。

## 8. 变更记录

| 日期 | 说明 |
|---|---|
| 2026-07-07 | 修复 SSE 最终事件、会话归属校验、逻辑删除、工具上下文回填、工具日志写入和 URL 校验问题。 |
| 2026-07-15 | 同步 MCP 服务、MCP 工具、工具分类、交互式提问、内建 `ask_user` 工具和风险审查能力。 |
| 2026-07-15 | 移除未确认的未来版本、限流/监控/压测等任务规划；保留工作流和知识库预留功能。 |
| 2026-07-15 | 新增 PostgreSQL 16 + pgvector 迁移脚本、pgloader 工具链和一次切换运行手册。 |
