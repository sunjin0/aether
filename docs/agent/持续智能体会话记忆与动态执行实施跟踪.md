# 持续智能体会话记忆与动态执行实施跟踪

本文跟踪[重构方案](持续智能体会话记忆与动态执行重构方案.md)的真实交付状态。状态以代码、迁移和可执行验证为准，不以设计文档或前端临时状态为准。

## 当前范围

| 阶段 | 状态 | 本仓库已交付 |
| --- | --- | --- |
| Phase 0：边界固化 | 已完成 | `session_id`、`task_id`、`attempt_no` 已写入 `agent_run`；任务与审批/提问的交互协议已分离。 |
| Phase 1：会话记忆可用 | 进行中 | Session、Task、事件、记忆表及查询/控制 API 已落地；历史 `agent_run` 的回填迁移为 V71。 |
| Phase 2：动态任务运行时 | 进行中 | Task 状态机、Session 串行队列、计划版本投影、继续/目标变更路由和计划查询已落地。 |
| Phase 3：长期记忆与能力编排 | 进行中 | 已支持任务结论记忆、用户显式偏好注入、Skill 冻结快照和工具委派范围；跨 Task 向量检索与统一行动模型待补。 |
| Phase 4：运营与治理 | 进行中 | Session/Task 指标、记忆删除、保留期清理和用户反馈 API 已落地；运营大盘与告警待补。 |

## 本仓库的已验证交付

- Flyway V64–V71：Session、Task、计划、事件、记忆、运行尝试、图线程与历史数据回填。
- `POST /api/agent/session/conversation/{conversationId}/messages`：在既有 Session 内创建或继续 Deep Task，服务端冻结 Skill、检索与用户身份上下文，返回 `sessionId`、`taskId`、`runId`。
- Session 查询、时间线、Task 计划/事件、暂停、继续、`ask_user` 输入、记忆管理及指标接口均在 `/api/agent/session` 下。
- 全栈 Compose 中 Deep Agent 已等待 PostgreSQL 健康检查后启动。

已执行验证：

```text
mvn -pl admin -am -DskipTests compile
# 成功；V71 已复制至 api/target/classes/db/migration/postgresql/

mvn -pl biz -am -Dtest=AgentTaskServiceImplTest,DeepAgentRunServiceTest,AgentRunPlanServiceImplTest ... test
# 23 项测试通过

docker compose -f docker-compose.all.yml config --quiet
# 成功
```

## 关联仓库的交付状态

下列代码不在本仓库，需与本仓库 API 一起发布和验收：

1. `aether-deep-agent-service`（已交付，commit `2f69f88`）
   - 以 `session_id` 作为 LangGraph 图线程，`run_id` 仅作为执行尝试；
   - 提供 `/v1/sessions/{sessionId}/tasks`、恢复及状态查询的兼容路由；
   - 已验证 PostgreSQL 检查点、pending interaction 与 outbox 在服务重启后的恢复和幂等重投（`tests/test_recovery.py`，全套 47 项测试通过）。
2. `aether-dashboard`（已交付）
   - 聊天页先加载 Session 快照与时间线，再订阅 SSE 增量；刷新后重新订阅仍可推进运行的增量；
   - 右侧工作区展示当前 Task、计划版本（含变更原因）、步骤结果摘要、暂停/继续和用户输入入口；
   - 刷新后以 Session/Task/时间线 API 历史状态恢复工作区，不以前端内存为任务状态来源。
3. `aether-mcp-server`（已交付，commit `093ce3c`）
   - `generate_artifact` 动作幂等键：同一运行内重试相同参数的工具调用直接返回已确认结果，避免重复提交渲染任务；
   - `GET /metrics` 暴露 `idempotencyHits` / `submittedActions` 指标；
   - MCP、Sandbox、工作流、Skill 的统一行动描述与风险策略仍可在后续按需扩展（风险分析已由 Admin `ToolCallRiskAnalyzer` 承担）。

## 发布前验收

1. 在测试 PostgreSQL 执行 Flyway V71，核验历史运行均有对应 Session/Task，且已关联的新运行不被改写。
2. 完成一个含工具调用的多步骤 Task，暂停并重启 Deep Agent 后继续；已完成副作用动作不得重复。
3. 刷新 Dashboard，验证同一会话的当前任务、历史计划、等待用户输入与记忆摘要均能恢复。
4. 使用其他用户令牌访问 Session、Task、记忆和输入接口，必须被拒绝。

### 验收结果（2026-08-16，本地 Compose 全栈）

- **#1 已通过**：Flyway V57–V71 全部 `success=true`；339 条历史 `agent_run` 全部关联 `session_id`/`task_id`/`attempt_no`，无孤儿 Task。
- **#2 已通过**：真实 Deep 工具任务（`get_current_time` × 多轮）经审批→暂停→`docker restart aether-deep-agent`→继续，最终 COMPLETED；计划版本出现 `RESUME`（v3），暂停前已完成的工具调用（时间戳 17:16:55）重启后未重复执行。
- **#3 已通过（数据层）**：Dashboard 为本地构建（chat bundle 含 `session/conversation`）；经 nginx 代理路径，会话快照（8 任务）、时间线（28 事件）、任务计划（11 版本，含 `SUPERSEDED`/`RESUME`/`COMPLETED` reason）、会话记忆（3 条任务结论）均能恢复。
- **#4 部分通过**：有效 `manger` token 可访问自有 Session；过期/非法 token 在认证层被拒（`code=401`）；控制器 `requireOwnedSession`/`requireOwnedTask` 已实现归属校验（非归属 404）。因数据库无第二个有效用户 token，未做"有效其他用户 token 拒绝"的在线复测。
- **修复的 bug**：
  - `AgentSessionController.submitMessage` 向 `knowledgeContext.enhance()` 传入不可变列表，触发 `UnsupportedOperationException`；改为可变 `ArrayList`（commit `89b9941`）。
  - `DeepAgentRunService.markFailed` 将超长错误写入 `error_msg varchar(1024)` 触发 `value too long`；改为截断到 1024（commit `89b9941`）。
  - Deep Agent 恢复待审批任务时空 decisions/answers 会报 `0 decisions vs 1 hanging tool call`；改为重新持久化交互并重新投递 `tool.approval.required`/`ask_user.required`，保持等待状态（commit `0f6f9f3`，含 2 条新测试，全套 49 项通过）。
- **Dashboard 右侧工作区**：记忆/概览入口移入右侧工作区头部；记忆弹窗改为卡片布局（重要度、来源任务、可滚动正文）；近期事件状态前缀映射为国际化文案（commit `4ba7f80`）。
- **Deep Agent 模型配置对齐 Admin**：Admin 新增签名接口 `GET /api/agent/deep-runs/model-config/{agentId}` 返回 model/baseUrl/apiKey（commit `9e7129d`）；Deep Agent 运行时按 agentId 拉取并在内存使用，apiKey 不落库，Admin 不可达时回退环境变量（commit `8c55dfa`）；修正接口路径与模型名 `openai:` 前缀规范化（commit `0315b17`）；agent 设置页 DEEP 模式开放模型设置（commit `bbc48bb`）。已在真实任务上验证：`fetch_model_config` 成功、`init_chat_model(openai:deepseek-v4-flash, api.deepseek.com, admin apiKey)` 正常执行并完成工具任务。
- **任务规划对齐 Codex/Claude（计划先行+确认 / 勾选清单 / 逐步展示 / 动态调整）**：deep-agent 生成初始计划后暂停并发 `plan.approval.required`，批准后才执行（commit `0c32f34`）；Admin 生成 `deep_plan_approval` 交互卡片并处理确认恢复（commit `6e746f5`）；dashboard 工作区改可勾选清单、`InteractiveQuestionCard` 新增计划确认卡片（commit `b0d87a1`）。真实任务验证：任务进入 `WAITING_APPROVAL：等待计划确认` → 批准 → `RUNNING` → `COMPLETED`。动态调整通过聊天输入框的"改为/补充"目标变更路由触发重规划。
- **Codex 风格中断（移除手动暂停/继续）**：修复计划确认卡片 `config.questions[0]` undefined 报错；工作区移除"暂停/继续"按钮，改为"中断"按钮 + 已中断提示；中断后通过回复"继续/改为/重新"复用任务继续或调整目标（`resolveTaskRoute` 补充"重新/重做"关键词）。commits `bdcf680`(dashboard) + `6b25671`(admin)。
- **方向 B：计划驱动执行 + 步骤验证（重新定义 Deep 定位）**：Deep = 任务执行智能体（标准 Agent 回答，Deep 做事）。计划作为执行契约注入指令（按序执行、逐步验证、`[STEP_VERIFIED]` 标记）；executor 解析标记并发出 `step.verified`，缺省回退工具结果摘要；Admin 将 `step.verified` 写入计划步骤 `resultSummary` 并置 COMPLETED；dashboard 展示每步验证摘要。修复计划批准后直接执行（避免 `continue_from_checkpoint` 的 EmptyInputError）与 plan_approved 分支顺序。commits `8677798`、`57a5e83`、`f1ec784`、`1dc40c6`(deep-agent)、`fddc67d`(admin)、`b9b457a`(dashboard)。真实任务验证：计划批准 → 逐步执行 → 4 个 `step.verified` 事件 → 计划步骤全部 COMPLETED（验证摘要为通用回退，真实多步工具任务会更具体）。deep-agent 全套 55 项测试通过。
- **MCP 幂等镜像已部署**：`aether-mcp-server` Dockerfile 改用阿里云 apt + 清华 PyPI 镜像修复构建网络超时（commit `7c5e495`）；镜像重建成功，`/metrics` 幂等指标已上线（commit `093ce3c` 的改动生效）。
- **Dashboard 国际化与布局**：会话工作区文本全部迁入 `pages.agent.chat.session.*` 中英文键（commit `802f025`），任务/版本标题弹性省略、头部与时间线样式优化。
